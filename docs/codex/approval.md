# Approval

Fluxo:

``` text
Agent proposes
→ User reviews
→ Approve / Reject
→ Apply only after approval
```

A interface não deve apenas mudar estado visualmente. A decisão deve ser
persistida e validada no backend.
